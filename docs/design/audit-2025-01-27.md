# 功能审计报告（2025-01-27）

> 对比 `requirements.md` / `defects.md` 与当前代码实现，识别所有缺失/未完成的功能。

## 执行摘要

- **审计范围**：master 分支（commit 4f91e22）
- **审计时间**：2025-01-27
- **审计方法**：需求文档 vs 代码实现 + Git 历史回溯

## 一、已实现的功能（✅）

### M0 + M1 核心功能
- ✅ Tauri 2 桌面壳 + React 前端
- ✅ MySQL 异步驱动（mysql_async + SSH 隧道）
- ✅ 项目/工作区管理
- ✅ 连接管理（新建/编辑/删除/连接/断开）
- ✅ Schema 树（项目→连接→数据库→表→列）
- ✅ SQL 编辑器（CodeMirror 6）
- ✅ 查询执行（流式 + 取消）
- ✅ 结果网格（虚拟滚动）
- ✅ 历史记录（SQLite + FTS5）
- ✅ 数据编辑（单元格编辑 + UPDATE）
- ✅ 导出功能（CSV/JSON/SQL）
- ✅ 危险操作确认（guard_dangerous）
- ✅ 凭据管理（OS keyring）
- ✅ 多标签页
- ✅ 事务控制
- ✅ i18n（zh-CN/en-US）

### R11 连接生命周期重构
- ✅ 连接自动保存（config_id 关联）
- ✅ 断开不删除连接
- ✅ 连接右键菜单"打开/关闭连接"
- ✅ 移除"已保存连接"专栏

### R12 右键菜单完善（简化版）
- ✅ 全局屏蔽浏览器原生右键菜单
- ✅ 表节点右键菜单（重命名/删除/复制名称/清空表/查看 DDL/编辑表结构）
- ✅ ResultsGrid 右键菜单（复制单元格/行/INSERT/设 NULL）
- ✅ 后端命令：drop_view/drop_routine/drop_trigger/truncate_table/build_insert_sql

### R13 表管理与数据编辑增强
- ✅ 表右键菜单"查看 DDL"（插入编辑器）
- ✅ 可视化表结构编辑器（ALTER TABLE）
- ✅ ResultsGrid 工具栏（新增/删除/保存/刷新 + 行多选）
- ✅ 后端命令：show_create_table/get_table_structure/alter_table/get_primary_key/batch_delete_rows/batch_insert_rows

### 档位 C 缺陷（10 个方案组）
- ✅ #9: SSH TOFU（known_hosts）
- ✅ #22: SSH 凭据 keyring
- ✅ #48: CSP + ACL 门控
- ✅ #1: Value 类型映射（带 tag 的 envelope）
- ✅ #4: 方言感知 SQL（Dialect trait）
- ✅ #5: 取消秒断（流式通道）
- ✅ #24: SSH 隧道生命周期
- ✅ #28: 多结果集边界事件
- ✅ #45: 数值精度（i64/u64/decimal 字符串）
- ✅ #18: i18n 框架

## 二、缺失的功能（❌）

### R12 完整版：Schema 树高级节点
**状态**：未实现（简化版只支持 table/column）

#### 缺失的列表命令
- ❌ `list_views(conn_id, database)` → `Vec<ViewInfo>`
- ❌ `list_functions(conn_id, database)` → `Vec<RoutineInfo>`
- ❌ `list_procedures(conn_id, database)` → `Vec<RoutineInfo>`
- ❌ `list_triggers(conn_id, database, table?)` → `Vec<TriggerInfo>`

#### 缺失的 Schema 树节点类型
```typescript
// 当前只有：conn / db / table / column
// 需要补充：
type NodeKey = 
  | { kind: "view"; connId: number; db: string; view: string }
  | { kind: "function"; connId: number; db: string; name: string }
  | { kind: "procedure"; connId: number; db: string; name: string }
  | { kind: "trigger"; connId: number; db: string; table?: string; name: string }
```

#### 缺失的右键菜单项
- ❌ 视图节点：查看定义/删除视图（后端 `drop_view` 已存在）
- ❌ 函数/存储过程节点：查看定义/删除（后端 `drop_routine` 已存在）
- ❌ 触发器节点：查看定义/删除（后端 `drop_trigger` 已存在）

#### 后端已存在但未暴露的能力
- ✅ `dby-core`: `drop_view/drop_routine/drop_trigger` DDL 函数
- ✅ `commands.rs`: `drop_view/drop_routine/drop_trigger` 命令
- ❌ 前端 API 封装缺失
- ❌ SchemaTree 组件未集成

### 其他潜在缺失（需用户确认）
- ❓ 连接配置编辑功能（#63：连接配置无更新命令）
- ❓ 数据库节点右键菜单项（创建数据库/删除数据库/刷新）
- ❓ 列节点右键菜单项（复制列名/查看列定义）

## 三、档位 B/A 待修复缺陷

### 档位 B —— 需评审（29 项）
按 P 级排序：

#### P1 高优（3 项）
- #25: 编辑/危险确认模态框不绑定连接
- #26: 数据编辑来源未校验（可误改数据）
- #44: 启动加载健壮性（config 重置/history panic）

#### P2 中优（23 项）
- #2: CRUD 用 `window.prompt/confirm`
- #3: 树节点 key 用 `:` 拼接再 split（✅ 已修复：现用 JSON.stringify）
- #6: 连接自动持久化、无「保存连接」开关（✅ R11 已修复）
- #8: SSH 仅密码认证
- #11: 数据编辑值类型在前端正则解析
- #12: 危险操作关键词误报
- #13: 历史写入同步
- #14: 语句库删除未实现
- #19: 错误处理不一致（✅ 已修复：unified errToString）
- #27: 流式 truncated/info 未接线 + 行追加无上限
- #29: `DbError` 跨 IPC 丢失错误种类
- #30: `Settings` 字段已建模但未接线
- #31: 历史 `clear` 不清理 FTS + 跨项目归因有损
- #35: 流式忽略 `max_rows` + 每查询 `USE`
- #36: SSH 无连接超时 + direct-tcpip 失败被吞
- #37: `begin` 嵌套隐式提交 + autocommit 无状态
- #38: 导出无行数上限、整串过 IPC
- #41: `delete_project` 遗留孤儿配置与钥匙串
- #42: 流式通道无终止/错误事件 + `send` 失败被吞
- #47: 命令面不完整（`Connection` 能力无 command）
- #50: 能力矩阵未在前端消费
- #69: 编辑控件类型覆盖不足

#### P3 低优（3 项）
- #7: 关闭标签 = 断开连接
- #56: 语句库去重按字面量而非参数化
- #59: `split_statements` 不支持方言差异
- #68: 可访问性缺失

### 档位 A —— 直接修（24 项）

#### P2 中优（7 项）
- #17: 查询超时未接线
- #40: `USE db` 每查询执行
- #43: `connect` 半失败留孤儿连接
- #49: connect/reconnect 依赖「列表末尾」启发式（✅ R11 已修复）
- #53: 启动不 `listConnections()`
- #58: `config.json` 非原子写
- #63: 连接配置无更新命令 / `color` 死字段
- #66: SchemaTree 缓存无失效

#### P3 低优（17 项）
- #10: 结果网格列宽固定
- #15: workspace 残留未用字段
- #16: 切换项目丢失活动连接
- #39: 历史/钥匙串写入错误被吞
- #46: `export_result` format 校验前执行查询
- #51: `analyze_danger` 的 `warn` 级被忽略
- #52: 历史搜索无防抖
- #54: tabs 与项目过滤不一致
- #55: `String(e)` 错误串化（✅ 已修复）
- #57: `DangerLevel::Warn` 死代码
- #61: 元数据 SQL 字符串拼接
- #62: tokio `time` feature 未显式声明
- #64: `resultVersion` 死字段
- #65: `mutateResult` 就地 mutate
- #67: 全局 `status` 无 per-connection 语义

## 四、优先级建议

### 第一优先级：完成 R12 完整版（Schema 树高级节点）
**理由**：
1. 后端 DDL 函数已存在（drop_view/drop_routine/drop_trigger）
2. 只需补充列表命令 + 前端展示逻辑
3. 完成度：70%（删除已实现，只缺列表和 UI）
4. 用户明确提到功能丢失

**工作量估算**：中型任务（2-3 小时）
- 后端：4 个列表命令（list_views/list_functions/list_procedures/list_triggers）
- 前端：扩展 SchemaTree 节点类型 + 右键菜单
- i18n：补充翻译键

### 第二优先级：档位 B P1 高优缺陷（3 项）
- #25: 编辑/危险确认模态框不绑定连接
- #26: 数据编辑来源未校验
- #44: 启动加载健壮性

### 第三优先级：档位 A P2 中优缺陷（易修且影响体验）
- #17: 查询超时未接线
- #40: `USE db` 每查询执行
- #43: `connect` 半失败留孤儿连接
- #53: 启动不 `listConnections()`
- #63: 连接配置无更新命令

## 五、建议行动

### 立即行动（用户确认后）
1. **实施 R12 完整版（Schema 树高级节点）**
   - 按工作流铁律：中型任务 → 先写设计文档 → 评审通过 → 单代理串行实现
   - 估计时间：2-3 小时
   - 文件清单：
     - `crates/dby-core/src/metadata.rs`: 新增 list_views/list_functions/list_procedures/list_triggers
     - `src-tauri/src/commands.rs`: 封装 4 个命令
     - `src/api.ts`: TypeScript 类型 + 封装
     - `src/components/SchemaTree.tsx`: 扩展节点类型 + 右键菜单
     - `src/locales/*.json`: i18n 翻译

2. **验证其他可能的丢失功能**
   - 用户确认：连接配置编辑功能是否存在？
   - 用户确认：数据库/列节点右键菜单是否完整？

### 后续排期
- 档位 B P1（3 项）→ 档位 A P2（7 项）→ 档位 B P2（按影响排序）

## 六、附录：Git 历史验证

### 关键提交
- `1dfea3f`: feat(tauri): 新增 drop_view/drop_routine/drop_trigger/truncate_table 命令
- `8738952`: feat(core): 新增 drop_view/drop_routine/drop_trigger/truncate_table DDL 函数
- `4f91e22`: feat(R11): 连接生命周期重构 - 自动保存与重连
- `e3abb4a`: Merge pull request #1 from simmszhang/feat/table-management-r13

### 分支状态
- `master`: 4f91e22（包含 R11 + R13）
- `feat/table-management-r13`: d2842f7（已合并到 master）
- worktree: `.worktrees/feat/context-menu-r12` → feat/table-management-r13

**结论**：没有代码回退，R12 本身就是简化版实现（设计文档明确说明）。
